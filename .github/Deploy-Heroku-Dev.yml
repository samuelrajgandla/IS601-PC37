# Your workflow name.
name: Deploy to heroku dev.

# Run workflow on every push to master branch.

on:
  push:
    branches:
      - dev
      - Flask_sample

# Your workflows jobs.
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      DB_URL: ${{secrets.mysql://sg2434:R4hDlTmLkqCn@db.ethereallab.app:3306/sg2434}}
      PORT: 8080
      # change to the project directory you're deploying
      WORKING_DIRECTORY: flask_sample
    defaults:
      run:
          working-directory: ${{env.WORKING_DIRECTORY}}
    
    steps:
      # Check-out your repository.
      - name: Checkout
        uses: actions/checkout@v2
      - name: Run tests
        run: pip install -r requirements.txt && pytest

### ⬇ IMPORTANT PART ⬇ ###

      - name: Build, Push and Release a Docker container to Heroku. # Your custom step name
        uses: gonuit/heroku-docker-deploy@v1.3.3 # GitHub action name (leave it as it is).
        with:
          # Below you must provide variables for your Heroku app.

          # The email address associated with your Heroku account.
          # If you don't want to use repository secrets (which is recommended) you can do:
          # email: my.email@example.com
          email: ${{ secrets.sg2434@njit.edu }}
          
          # Heroku API key associated with provided user's email.
          # Api Key is available under your Heroku account settings.
          heroku_api_key: ${{ secrets.18eeb771-3c00-47b3-a880-e2a3758a55f7 }}
          
          # Name of the heroku application to which the build is to be sent.
          heroku_app_name: ${{ secrets.is601-sg2434-dev }}

          # (Optional, default: "./")
          # Dockerfile directory.
          # For example, if you have a Dockerfile in the root of your project, leave it as follows:
          dockerfile_directory: ${{env.WORKING_DIRECTORY}}

          # (Optional, default: "Dockerfile")
          # Dockerfile name.
          dockerfile_name: Dockerfile

          # (Optional, default: "")
          # Additional options of docker build command.
          docker_options: "--no-cache"
          
          # (Optional, default: "web")
          # Select the process type for which you want the docker container to be uploaded.
          # By default, this argument is set to "web".
          # For more information look at https://devcenter.heroku.com/articles/process-model
          process_type: web
          
   
          
### ⬆ IMPORTANT PART ⬆ ###